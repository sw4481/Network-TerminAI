//! Pipe-filter syntax parsing lives primarily on the frontend (Phase 5).
//! This module is a placeholder for any future Rust-side helpers (e.g. a
//! JSON-server-side filter evaluator). The grammar lives in
//! `src/lib/structuredPipe.ts`.

#[allow(dead_code)]
pub const SENTINEL: &str = "↗structured.";
